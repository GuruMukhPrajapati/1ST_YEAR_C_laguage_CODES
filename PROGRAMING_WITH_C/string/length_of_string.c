#include <stdio.h>

int main() {
    char st[100]; // declare an array to store the string
    int i, c = 0;
    printf("Enter a string: ");
    scanf("%s", st);
    for (i = 0; st[i] != '\0'; i++) {
        c = c + 1;
    }
    printf("Length is: %d\n", c);
    return 0;
}
hell o fas[sd]  dssd
n sdcsdh STDIN_FILENOb sdfkh _DISABLE_MSVC_PRINTF_QUIRKSs STDIN_FILENOsd ds
 sd
  #include <stdio.h>
  
  int main(){

  return 0;
  }
